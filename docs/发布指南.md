# 📦 npm 发布指南 - v1.6.0

## ✅ 发布前准备（已完成）

- ✅ 版本号已更新：`1.5.0` → `1.6.0`
- ✅ CHANGELOG.md 已更新
- ✅ 打包测试通过（24个文件，49.7 kB）

## 🚀 发布步骤

### 步骤 1: 登录 npm

**方式一：命令行登录（推荐）**
```bash
npm login
```

然后输入：
- **Username**: 你的npm用户名
- **Password**: 你的npm密码
- **Email**: 你的邮箱地址
- **OTP**: 如果启用了双因素认证，输入验证码

**方式二：使用访问令牌**
```bash
npm config set //registry.npmjs.org/:_authToken YOUR_TOKEN_HERE
```

### 步骤 2: 验证登录状态

```bash
npm whoami
```

应该显示你的npm用户名。

### 步骤 3: 检查包信息

```bash
# 查看npm上的当前版本
npm view build-deploy-tools version

# 查看包信息
npm view build-deploy-tools
```

### 步骤 4: 发布到 npm

**直接发布：**
```bash
npm publish
```

**或者使用发布脚本：**
```bash
# Windows
publish.bat

# Linux/Mac
chmod +x publish.sh
./publish.sh
```

### 步骤 5: 验证发布

发布成功后，访问：
- https://www.npmjs.com/package/build-deploy-tools

或使用命令验证：
```bash
npm view build-deploy-tools version
npm info build-deploy-tools
```

## 📋 当前状态

- **本地版本**: 1.6.0
- **npm最新版本**: 1.5.0
- **状态**: ⏳ 等待登录后发布

## ⚠️ 注意事项

1. **权限问题**: 确保你有发布 `build-deploy-tools` 包的权限
2. **包名冲突**: 如果包名已被占用，需要修改 package.json 中的 name
3. **版本号**: 确保版本号大于npm上的最新版本
4. **Token过期**: 如果token过期，需要重新登录

## 🔧 故障排除

### 问题1: 401 Unauthorized
**解决**: 运行 `npm login` 重新登录

### 问题2: 403 Forbidden
**解决**: 检查你是否是该包的维护者，或者包名是否已被占用

### 问题3: 404 Not Found
**解决**: 
- 如果是第一次发布，确保包名唯一
- 如果包已存在，检查是否有发布权限

### 问题4: Token过期
**解决**: 
```bash
npm logout
npm login
```

---

**下一步**: 请运行 `npm login` 登录后，再执行 `npm publish` 发布。
